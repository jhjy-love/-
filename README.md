# 5D Graphics Demo

This repository contains a small Python script demonstrating how to visualize five-dimensional data. Three dimensions are mapped to the X, Y, and Z axes, while color and point size encode the fourth and fifth dimensions.

## Usage

Run the following command to generate an example plot saved as `5d_plot.png`:

```bash
python five_d_plot.py
```
